{
  "name": "Group12",
  "version": "1.0.0",
  "description": "",
  "main": "hello.js",
  "scripts": {
    "test": "jest",
    "coveralls": "cat ./coverage/lcov.info | node node_modules/.bin/coveralls",
    "test:coverage": "jest --coverage",
    "test:coveralls": "jest --coverage --coverageReporters=text-lcv | coveralls"
  },
  "jest": {
    "collectCoverage": true,
    "coverageReporters": [
      "html"
    ]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jest": "^27.0.4"
  },
  "devDependencies": {
    "coveralls": "^3.1.0"
  }
}